<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>
			{% block title %}Symfony Blog
			{% endblock %}
		</title>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
		{% block stylesheets %}
			<style>
				#comments .card {
					transition: transform 0.2s, box-shadow 0.2s;
				}
				#comments .card:hover {
					transform: translateY(-2px);
					box-shadow: 0 4px 8px rgba(0, 0, 0, 0.15) !important;
				}
				.card-header {
					font-weight: 500;
				}
			</style>
		{% endblock %}
	</head>
	<body>
		<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
			<div class="container">
				<a class="navbar-brand" href="{{ path('article_index') }}">📰 Symfony Blog</a>
				<div class="navbar-nav ms-auto">
					{% if app.user %}
						<span class="navbar-text me-3">👤
							{{ app.user.username }}</span>
						<a class="btn btn-outline-light btn-sm" href="{{ path('article_new') }}">➕ Nouvel article</a>
						<a class="btn btn-outline-light btn-sm ms-2" href="{{ path('app_logout') }}">🚪 Déconnexion</a>
					{% else %}
						<a class="btn btn-outline-light btn-sm" href="{{ path('app_login') }}">🔐 Connexion</a>
					{% endif %}
				</div>
			</div>
		</nav>

		<div class="container mt-4">
			{% for message in app.flashes('success') %}
				<div class="alert alert-success alert-dismissible fade show">
					{{ message }}
					<button type="button" class="btn-close" data-bs-dismiss="alert"></button>
				</div>
			{% endfor %}

			{% for message in app.flashes('error') %}
				<div class="alert alert-danger alert-dismissible fade show">
					{{ message }}
					<button type="button" class="btn-close" data-bs-dismiss="alert"></button>
				</div>
			{% endfor %}

			{% block body %}{% endblock %}
		</div>

		 <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
		{% block javascripts %}{% endblock %}
	</body>
</html>
