{{ form_start(form) }}
    <div class="mb-3">
        {{ form_label(form.content, 'Votre commentaire') }}
        {{ form_widget(form.content, {'attr': {'class': 'form-control', 'rows': 4, 'placeholder': 'Écrivez votre commentaire ici...'}}) }}
        {{ form_errors(form.content) }}
    </div>

    <button type="submit" class="btn btn-primary">
        💬 Publier le commentaire
    </button>
{{ form_end(form) }}